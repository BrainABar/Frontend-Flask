"""
    Adds project path using > pip install --editable path/to/project
    Removes need to use os/sys or edit PYTHONPATH when using imports in our containers/virtual enviroments
"""
from setuptools import setup


setup()
